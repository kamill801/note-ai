{
  "name": "@note-ai/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend API for Note AI MVP1.",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "smoke": "node scripts/health-smoke.ts",
    "test": "node --test tests/*.test.ts",
    "db:validate": "node scripts/validate-migrations.ts",
    "db:check": "node scripts/check-db-connection.ts",
    "typecheck": "tsc --noEmit",
    "smoke:mvp1": "node scripts/mvp1-smoke.ts"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.9.3"
  }
}
